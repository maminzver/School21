#SESSION1
BEGIN TRANSACTION ISOLATION LEVEL READ COMMITTED;

#SESSION2
BEGIN TRANSACTION ISOLATION LEVEL READ COMMITTED

#SESSION1
SELECT SUM(rating)
FROM pizzeria;

SESSION2
INSERT INTO pizzeria VALUES (10, 'Kazan Pizza', 5);

COMMIT;

#SESSION1
SELECT SUM(rating)
FROM pizzeria;

COMMIT;

SELECT SUM(rating)
FROM pizzeria;

#SESSION2
SELECT SUM(rating)
FROM pizzeria;