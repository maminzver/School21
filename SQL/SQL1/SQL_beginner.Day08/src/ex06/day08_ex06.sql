#SESSION1
BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ;

#SESSION2
BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ

#SESSION1
SELECT SUM(rating)
FROM pizzeria;

SESSION2
INSERT INTO pizzeria VALUES (11, 'Kazan Pizza 2', 4);

COMMIT;

#SESSION1
SELECT SUM(rating)
FROM pizzeria;

COMMIT;

SELECT SUM(rating)
FROM pizzeria;

#SESSION2
SELECT SUM(rating)
FROM pizzeria;